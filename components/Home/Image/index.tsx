import React from 'react'
import cx from 'classnames'
import styles from './image.module.scss'

function ImageContainer() {
  return <div className={cx(styles.ImageBackground)} />
}

export default ImageContainer
