import { motion } from "framer-motion";
import styled from "styled-components";

export const ExperienceLayer = styled(motion.div)`
  width: 100vw;
  min-height: 100vh;
  @media screen and (min-width: 768px) {
  }
`;
